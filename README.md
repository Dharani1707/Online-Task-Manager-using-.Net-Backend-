# Online-Task-Manager-using-.Net-Backend-
An online task manager with a .NET backend uses ASP.NET Core to handle business logic, authentication, and API endpoints. It stores tasks, users, and statuses in databases like SQL Server and exposes RESTful APIs. The backend manages CRUD operations (create, read, update, delete) for tasks and supports role-based access. 
